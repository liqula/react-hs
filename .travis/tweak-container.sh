#!/bin/bash

set -e
cd `dirname $0`
export PATH=$HOME/.local/bin:$HOME/bin:$PATH

# apply some tweaks to the docker container that will be fixed with the next re-build go here.

true  # nothing to do at this time.
